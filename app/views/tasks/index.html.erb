<h1>Tasks</h1>
<% @tasks.each do |task| %>
  <% if task.completed %>
    <input type="checkbox" name="<%= task.title %>" value="<%= task.title %>" checked>
    <label for="<%= task.title %>"> <%= task.title %> - <%= task.details %></label>
    <br>
  <% else %>
    <input type="checkbox" name="<%= task.title %>" value="<%= task.title %>">
    <label for="<%= task.title %>"> <%= task.title %> - <%= task.details %></label>
    <br>
  <% end %>
<% end %>
